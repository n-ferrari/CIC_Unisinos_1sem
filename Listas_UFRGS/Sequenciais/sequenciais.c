#include<stdio.h>
#include<math.h>
/*
//1. Calculates distance between 2 points in xy plane
int main()
{
    float x1, x2, y1, y2;
    float dist;

    printf("Enter x1, y1, x2, y2: ");
    scanf("%f", &x1);
    scanf("%f", &y1);
    scanf("%f", &x2);
    scanf("%f", &y2);

    dist = sqrt(((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1)));
    printf("The distance between 2 points is %f",dist);

}


// 2. Convert a price from BRL to USD
int main()
{
    float brl, usd, exchange_rate;

    printf("Which is the value in brl? ");
    scanf("%f", &brl);
    printf("Which is the exchange rate brl/usd? ");
    scanf("%f", &exchange_rate);

    usd = brl / exchange_rate;

    printf("The value in usd is $ %.3f",usd);
}


// 3. Calculate arithmetic and harmonic average from 3 values
int main()
{
    float x, y, z, average, harmonic;

    printf("Enter 3 values: ");
    scanf("%f", &x);
    scanf("%f", &y);
    scanf("%f", &z);

    average = (x + y + z) / 3;
    harmonic = 3 / (1 / x + 1 / y + 1 / z);

    printf("The arithmetic average is %f and the harmonic average is %f", average, harmonic);

}
*/

// 
int main()
{

}
